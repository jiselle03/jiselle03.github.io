# jiselle03.github.io

Personal portfolio for Jiselle Liu, built with Astro and deployed as a static site.

## Commands

```sh
npm run dev
npm run build
npm run preview
```

## Deployment

The repo keeps `CNAME` for the custom domain. `npm run deploy` builds the static site and publishes `dist` with `gh-pages`.
