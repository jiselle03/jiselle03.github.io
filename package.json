{
  "name": "jiselle03.github.io",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "homepage": "https://jiselleliu.com",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build --force",
    "build:public": "npm run build && node scripts/strip-public-demos.mjs",
    "preview": "astro preview",
    "deploy": "npm run build:public && gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "gh-pages": "^6.3.0",
    "typescript": "^5.8.3"
  }
}
