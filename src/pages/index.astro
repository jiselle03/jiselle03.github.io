---
import '../styles/global.css';

const links = [
  { label: 'GitHub', href: 'https://github.com/jiselle03' },
  { label: 'LinkedIn', href: 'https://linkedin.com/in/jiselleliu' },
  { label: 'Email', href: 'mailto:hello@jiselleliu.com' },
];

const signals = [
  { label: 'Tutoring', value: 'Scheduling, calendars, role-based operations' },
  { label: 'AI hiring', value: 'Candidate workflows and decision support' },
  { label: 'Hum', value: 'Fitness tracking with calm progress loops' },
];

const ownershipNotes = [
  { label: 'Recent work', value: 'Owned product direction and shipped the build' },
  { label: 'Forward engineering', value: 'Streamlining company workflows with pragmatic AI' },
];

const preferredStack = ['TypeScript', 'Next.js', 'React', 'tRPC', 'Drizzle', 'PostgreSQL', 'Tailwind', 'OpenAI', 'Radix UI', 'Vitest'];
const alsoUsed = ['Claude Code', 'Anthropic', 'Vue', 'Ruby on Rails', 'Go', 'Express', 'MongoDB', 'React Query', 'Playwright'];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Jiselle Liu, fullstack engineer with a frontend focus." />
    <meta name="theme-color" content="#0b1d2a" />
    <link rel="icon" href="/icons/favicon_512.png" />
    <link rel="apple-touch-icon" href="/icons/favicon_512.png" />
    <link rel="manifest" href="/manifest.json" />
    <title>Jiselle Liu | Fullstack Engineer</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>

    <header class="site-header" aria-label="Site header">
      <a class="brand" href="/" aria-label="Jiselle Liu home">
        <span class="brand-mark" aria-hidden="true">JL</span>
        <span>Jiselle Liu</span>
      </a>

      <nav class="nav-links" aria-label="Primary navigation">
        {links.map((link) => (
          <a href={link.href} target={link.href.startsWith('http') ? '_blank' : undefined} rel={link.href.startsWith('http') ? 'noreferrer' : undefined}>
            {link.label}
          </a>
        ))}
      </nav>
    </header>

    <main id="main" class="page-shell">
      <section class="hero" aria-labelledby="hero-title">
        <div class="hero-copy">
          <p class="eyebrow">Fullstack engineer / frontend focus</p>
          <h1 id="hero-title">
            <span>Useful software,</span>
            <span>careful interfaces.</span>
          </h1>
          <p class="lede">
            I build across the stack, with a bias for the parts users touch:
            scheduling tools, AI-assisted hiring workflows, and personal software that makes progress feel easier to see.
          </p>

          <div class="hero-actions" aria-label="Primary links">
            <a class="button button-primary" href="mailto:hello@jiselleliu.com">Contact</a>
            <a class="button button-secondary" href="https://github.com/jiselle03" target="_blank" rel="noreferrer">GitHub</a>
          </div>
        </div>

        <div class="interface-board" aria-hidden="true">
          <div class="board-window window-a">
            <span></span>
            <span></span>
            <span></span>
          </div>
          <div class="board-window window-b">
            <span></span>
            <span></span>
          </div>
          <div class="board-cursor"></div>
        </div>
      </section>

      <section class="work-section" aria-labelledby="work-title">
        <div class="section-kicker">
          <p>Recent work</p>
          <h2 id="work-title">Product-heavy builds, frontend-first care.</h2>
        </div>

        <div class="ownership-strip" aria-label="Work style">
          {ownershipNotes.map((item) => (
            <p>
              <span>{item.label}</span>
              {item.value}
            </p>
          ))}
        </div>

        <div class="signal-grid">
          {signals.map((item) => (
            <article class="signal-card">
              <p>{item.label}</p>
              <h3>{item.value}</h3>
            </article>
          ))}
        </div>
      </section>

      <section class="stack-band" aria-label="Technology stack">
        <div>
          <p>Preferred stack</p>
          <ul>
            {preferredStack.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </div>

        <div>
          <p>Also used</p>
          <ul>
            {alsoUsed.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </div>
      </section>
    </main>
  </body>
</html>
